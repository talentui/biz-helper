{
  "name": "@talentui/biz-helper",
  "version": "0.0.0-development",
  "description": "helpers form legacy talentui and new helpers",
  "main": "lib/index.js",
  "author": {
    "name": "liguoming",
    "email": "liguoming@beisen.com"
  },
  "license": "MIT",
  "scripts": {
    "cm": "git-cz",
    "build": "rm -rf lib && babel src -d lib --copy-files",
    "prepublish": "yarn run build",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "dependencies": {
    "es6-promise": "^4.1.0",
    "isomorphic-fetch": "^2.2.1",
    "lodash.foreach": "^4.5.0",
    "url-search-params": "^0.9.0"
  },
  "devDependencies": {
    "@talentui/cz-package-changelog": "^1.0.0",
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.5.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "commitizen": "^2.9.6",
    "semantic-release": "^8.2.0"
  },
  "peerDependencies": {
    "immutable": "^3.8.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/talentui/biz-helper.git"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/@talentui/cz-package-changelog"
    }
  }
}
